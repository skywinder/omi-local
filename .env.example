# Optional ngrok inputs for start.command / scripts/local-mac.sh configure.
# Copy to .env with permissions 600, then fill both values from one ngrok account.
# Existing connections change only through scripts/local-mac.sh edit-connection.
NGROK_URL=
NGROK_AUTHTOKEN=
