# omiloc

Личная аудиотека на Mac: записи с Omi CV1, транскрипты и плеер с переходом по фразе.
Записью управляет приложение на iPhone. Аудио передаётся через ngrok;
хранение и распознавание остаются на Mac.

Откройте **[start.command](start.command)** или выполните `./start.command`
из папки проекта. Скрипт подготовит Mac, покажет домен и ключ для iPhone
и откроет аудиотеку. [Инструкция по запуску](docs/START.md).

После настройки команда **`omiloc`** открывает аудиотеку из любой папки в Terminal.
Для приёма новых записей запускайте `start.command`.

Папки с аудио и транскриптами можно открыть в Finder через свёрнутый пункт
**«Папки»** внизу боковой панели аудиотеки.

Нужны Mac с Apple Silicon, CV1 и отдельно установленное приложение на iPhone.
Для распознавания один раз [подготовьте модели](docs/LOCAL_STT.md).
Полная установка на чистом Mac ещё не проверена.

## Debug на iPhone: запуск и обновление

После [первичной подготовки iOS](docs/LOCAL_SETUP.md) подключите разблокированный
iPhone к Mac, включите Developer Mode и откройте Terminal в папке проекта.
Перед новой сборкой выполните `make test-offline` и `make test-transport-app`.

```bash
./start.command        # backend, ngrok и аудиотека; после запуска команда завершается
./dev-iphone.command   # Debug на iPhone и интерактивная консоль Flutter
```

Если сервисы Mac уже работают, достаточно `./dev-iphone.command`.
Скрипт использует существующую подпись и сохранённые настройки подключения,
проверяет готовую Debug-сборку или пересобирает её при изменении входов.
При запросе на iPhone разрешите доступ к локальной сети для отладчика.
Дождитесь строки **`Flutter run key commands`** и оставьте эту консоль открытой.

После сохранения изменений в Dart нажимайте клавишу в этой консоли, без Enter:

- `r` — hot reload: применить изменения, сохранив состояние приложения;
- `R` — hot restart: перезапустить Dart и сбросить состояние;
- `v` — открыть Flutter DevTools в браузере; `h` — показать все команды;
- `q` — завершить отладку и остановить приложение на iPhone.

Изменения не применяются автоматически от одного сохранения файла.
Swift, нативные плагины и параметры сборки требуют завершить сеанс и запустить
скрипт снова; зависимости и генерацию кода сначала обновите по инструкции iOS.
Debug запускайте скриптом, а не значком на телефоне; для запуска без отладчика
используется Profile-сборка. Для проверки готовности: `./dev-iphone.command --check`.

Не запускайте два сеанса Flutter для одного приложения одновременно.
Консоль агента не переносится автоматически в ваш Terminal: сначала завершите
прежний сеанс, затем запустите скрипт в своём окне.
[Подробности Debug и hot reload](docs/DEVELOPMENT.md#debug-на-iphone-и-hot-reload).

[Подключение iPhone](docs/NGROK.md) · [Распознавание](docs/LOCAL_STT.md) ·
[Ограничения](docs/TEMPORARY_DISABLED_FEATURES.md) · [Для разработчиков](docs/DEVELOPMENT.md).

Основан на [Omi от Based Hardware](https://github.com/BasedHardware/omi).
[Происхождение](UPSTREAM.md) · [MIT](LICENSE) · [Логотип](web-local/assets/omiloc.png).
