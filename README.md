# Omi Local

Локальная запись аудио с **Omi CV1 или микрофона iPhone** на Mac.
Основан на открытом [Omi от Based Hardware](https://github.com/BasedHardware/omi).

```text
Omi CV1 / микрофон iPhone → приложение → ngrok → backend на Mac → WAV
```

Рабочий сценарий подтверждён через мобильную сеть для обоих источников,
включая повторную запись после перезапуска туннеля. WAV сохранены и прослушаны
на Mac: речь разборчива, `decode_errors=0`. Формат — PCM16, mono, 16 kHz.

В режиме `offline` используются локальные Firebase emulators и Redis;
ключи облачных STT/LLM-провайдеров не нужны. Готовый WAV можно обработать
[локальным WhisperX](docs/LOCAL_STT.md) и открыть транскрипт с говорящими
и таймкодами в приложении. Автономная batch-загрузка пока не подключена.

## Начать работу

См. [настройку через ngrok](docs/NGROK.md) и
[iOS-приложение и прежний LAN-режим](docs/LOCAL_SETUP.md).
Проверенная конфигурация: Mac с Apple Silicon, iPhone 17 Pro и Omi CV1
с firmware 3.0.21. Скрипт iOS пока ограничен этой моделью телефона.

- `app/` — Flutter-приложение и iOS-конфигурация Personal Team.
- `backend/` — приём аудио, запись файлов и тесты.
- `scripts/dev-harness/` — запуск и диагностика локальных сервисов.

## Ограничения

Это MVP для разработки.
Для микрофона iPhone выключайте CV1; для завершения записи полностью закрывайте
приложение. CV1 пишет тихо, переключение источников может создавать короткие
дополнительные файлы. Сохранность записи при обрывах не гарантируется.

Ngrok использует HTTPS/WSS и отдельный ключ приложения; передача проходит через
ngrok, хранение остаётся на Mac. Прежний LAN-режим без TLS — только для доверенной
сети. Установка проверена в изолированном окружении текущего Mac, не на втором
чистом компьютере.

Часть общего upstream-кода сохранена для совместимости; поддерживаемый сценарий —
локальная запись аудио на Mac. Происхождение и лицензия: [UPSTREAM.md](UPSTREAM.md), [MIT](LICENSE).
