# Omi Local

Локальный прототип записи аудио с **Omi CV1** на Mac через приложение для iPhone.
Основан на открытом [Omi от Based Hardware](https://github.com/BasedHardware/omi).

```text
Omi CV1 → Bluetooth → iPhone → локальный backend на Mac → WAV (→ Whisper live → Transcript to app → Next features)
```

Рабочий сценарий подтверждён: backend принял запись
длительностью 14,9 секунды, сохранил WAV и технические метаданные; речь успешно
прослушана на Mac. Формат — PCM16, mono, 16 kHz.

В режиме `offline` используются локальные Firebase emulators и Redis;
ключи облачных STT/LLM-провайдеров не нужны. Распознавание речи на локальной машине происходит, 
но не интегрировано обратно и на этапе добавления.

## Начать работу

Новый транспорт: [связь iPhone с Mac через ngrok](docs/NGROK.md).
Установка отдельного окружения и публичная передача синтетического аудио проверены;
приёмка нового приложения на телефоне через мобильную сеть ещё не завершена.

См. [локальную настройку и проверку записи](docs/LOCAL_SETUP.md).
Проверенная конфигурация: Mac с Apple Silicon, iPhone 17 Pro и Omi CV1
с firmware 3.0.21. Скрипт iOS пока ограничен этой моделью телефона.

- `app/` — Flutter-приложение и iOS-конфигурация Personal Team.
- `backend/` — приём аудио, запись файлов и тесты.
- `scripts/dev-harness/` — запуск и диагностика локальных сервисов.

## Ограничения

Это MVP для разработки.
Для финализации текущей записи требуется закрыть приложение. 
Сетевые ограничения проверены программно. Для прежнего LAN-режима используйте
доверенную сеть: он работает без TLS. Режим ngrok использует HTTPS/WSS и отдельный
ключ приложения.

Часть общего upstream-кода сохранена для совместимости; поддерживаемый сценарий —
локальная запись CV1. Происхождение и лицензия: [UPSTREAM.md](UPSTREAM.md), [MIT](LICENSE).
