# Omi Local

Локальный прототип записи аудио с **Omi CV1** на Mac через приложение для iPhone.
Основан на открытом [Omi от Based Hardware](https://github.com/BasedHardware/omi).

```text
Omi CV1 → Bluetooth → iPhone → локальный backend на Mac → WAV
```

Рабочий сценарий подтверждён на физическом устройстве: backend принял запись
длительностью 14,9 секунды, сохранил WAV и технические метаданные; речь успешно
прослушана на Mac. Формат — PCM16, mono, 16 kHz. Сама запись в репозиторий не входит.

В режиме `offline` используются локальные Firebase emulators и Redis;
ключи облачных STT/LLM-провайдеров не нужны. Распознавание речи и транскрипты
в текущий MVP не входят.

## Начать работу

См. [локальную настройку и проверку записи](docs/LOCAL_SETUP.md).
Проверенная конфигурация: Mac с Apple Silicon, iPhone 17 Pro и Omi CV1
с firmware 3.0.21. Скрипт iOS пока ограничен этой моделью телефона.

- `app/` — Flutter-приложение и iOS-конфигурация Personal Team.
- `backend/` — приём аудио, запись файлов и тесты.
- `scripts/dev-harness/` — запуск и диагностика локальных сервисов.

## Ограничения

Это MVP для разработки. Три последовательные сессии, длительная запись,
восстановление после разрывов и установка на чистой машине ещё не проверены.
Для финализации текущей записи требуется закрыть приложение; звук сравнительно
тихий. Сетевые ограничения проверены программно, но отсутствие внешнего трафика
всего приложения на физическом iPhone ещё не доказано. Используйте доверенную LAN:
локальный транспорт работает без TLS.

Репозиторий содержит отдельный снимок мобильного приложения и backend без истории,
desktop/web-приложений, firmware, облачных deployment workflows и рабочих журналов.
Часть общего upstream-кода сохранена для совместимости; поддерживаемый сценарий —
локальная запись CV1. Происхождение и лицензия: [UPSTREAM.md](UPSTREAM.md), [MIT](LICENSE).
