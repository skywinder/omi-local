# Omi Local

Локальная запись аудио с **Omi CV1 или микрофона iPhone** на Mac.
Основан на открытом [Omi от Based Hardware](https://github.com/BasedHardware/omi).

```text
Omi CV1 / микрофон iPhone → приложение → ngrok → backend на Mac → WAV
```

Через мобильную сеть отдельно проверены CV1 (16,70 секунды) и микрофон iPhone
(19,19 секунды), включая запись после перезапуска туннеля. WAV сохранены на Mac,
речь разборчива, `decode_errors=0`. Формат — PCM16, mono, 16 kHz.

В режиме `offline` используются локальные Firebase emulators и Redis;
ключи облачных STT/LLM-провайдеров не нужны. STT, diarization и batch-интеграция
остаются на паузе и в этот релиз транспорта не входят.

## Начать работу

Начните с [настройки связи iPhone с Mac через ngrok](docs/NGROK.md).
Установка и повторная установка проверены в отдельном пустом окружении
текущего Mac; проверка на втором чистом Mac не проводилась.

См. [локальную настройку и проверку записи](docs/LOCAL_SETUP.md).
Проверенная конфигурация: Mac с Apple Silicon, iPhone 17 Pro и Omi CV1
с firmware 3.0.21. Скрипт iOS пока ограничен этой моделью телефона.

- `app/` — Flutter-приложение и iOS-конфигурация Personal Team.
- `backend/` — приём аудио, запись файлов и тесты.
- `scripts/dev-harness/` — запуск и диагностика локальных сервисов.

## Ограничения

Это MVP для разработки.
Источники проверены отдельно; для микрофона iPhone выключайте CV1.
Для финализации записи CV1 требуется полностью закрыть приложение.
Запись CV1 относительно тихая. Переключение источников может создавать короткие
дополнительные файлы; улучшение жизненного цикла записи остаётся вне релиза.
Сетевые ограничения проверены программно. Для прежнего LAN-режима используйте
доверенную сеть: он работает без TLS. Режим ngrok использует HTTPS/WSS и отдельный
ключ приложения.

Часть общего upstream-кода сохранена для совместимости; поддерживаемый сценарий —
локальная запись аудио на Mac. Происхождение и лицензия: [UPSTREAM.md](UPSTREAM.md), [MIT](LICENSE).
